{
  "name": "vovitech-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Spec-Driven Development workspace for iOS + Vercel",
  "scripts": {
    "test:all": "echo \"Error: no global tests specified\" && exit 1",
    "lint:all": "echo \"Error: no global linting specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  }
}
